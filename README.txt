Simple site where user may type text in the input and the text appears beneath as transformed to uppercase.
Reset button clears the input as well as the text area. This allows for starting againg.
React handle chande in the input and onClick event resetting both areas.
Settings of the site are for development only. 
They allow for easier learning React but are not suitable for production.